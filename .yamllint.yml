extends: default

ignore: |
  .git/
  **/bin/
  **/obj/
  **/build/
  **/node_modules/

rules:
  line-length: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  truthy:
    check-keys: false
